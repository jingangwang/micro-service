server:
  port: 8084
spring:
  application:
    name: hystrix-turbine
eureka:
  client:
    service-url:
      defaultZone: http://admin:admin12345@localhost:8761/eureka/
  instance:
    prefer-ip-address: true
turbine:
  app-config-list: eureka-consumer
  cluster-name-expression: "default"